@{
    ViewData["Title"] = "About";
}

<p>
    @foreach (KeyValuePair<string, string> pair in ViewBag.Message)
    {
        @pair.Key @pair.Value<br>
    }
    Processor Count : @Environment.ProcessorCount<br />
</p>